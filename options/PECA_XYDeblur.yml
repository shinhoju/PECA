# general settings
name: PECA-XYDeblur
model_type: StereoImageCleanModel
scale: 1
num_gpu: 1
manual_seed: 100

# dataset and data loader settings
datasets:
  train:
    name: TrainSet
    type: Dataset_RectifiedImage
    dataroot_gt: ./datasets/PECA/train/target_crops
    dataroot_lq: ./datasets/PECA/train/input_crops
    dataroot_gd: ./datasets/PECA/train/guide_crops

    geometric_augs: false
    filename_tmpl: '{}'
    io_backend:
      type: disk

    gt_size: 128
    
    # data loader
    use_shuffle: true
    num_worker_per_gpu: 8
    batch_size_per_gpu: 8
    dataset_enlarge_ratio: 1
    prefetch_mode: ~
    
  val:
    name: pilot-test
    type: Dataset_RectifiedImage
    dataroot_gt: ./datasets/PECA/test/target
    dataroot_lq: ./datasets/PECA/test/input
    dataroot_gd: ./datasets/PECA/test/guide
    io_backend:
      type: disk

network_g:
  type: StereoXYDeblur
  search_range: 5
  encs: ""
  decs: "3"
  enc_mode: sep
  use_res: true
  direction_mode: uni
  temperature: 0.01
  lin: true

# path
path:
  pretrain_network_g: ~
  strict_load_g: true
  resume_state: ~

# training settings
train:
  optim_g:
    type: AdamW
    lr: !!float 0.0001
    weight_decay: !!float 1e-3
    betas: [0.9, 0.9]

  scheduler:
    type: TrueCosineAnnealingLR
    T_max: 400000
    eta_min: !!float 1e-7

  mixing_augs:
    mixup: false
    mixup_beta: 1.2
    use_identity: true

  total_iter: 400000
  warmup_iter: -1 # no warm up
  use_grad_clip: false

  # losses
  pixel_opt:
    type: L1Loss
    loss_weight: 1
    reduction: mean

# validation settings
val:
  val_freq: !!float 1e4
  save_img: false

  metrics:
    psnr:
      type: calculate_psnr
      crop_border: 0
      test_y_channel: false
    ssim:
      type: calculate_ssim
      crop_border: 0
      test_y_channel: false

# logging settings
logger:
  print_freq: 1000
  save_checkpoint_freq: !!float 1e4
  use_tb_logger: true
  wandb:
    project: ~
    resume_id: ~

# dist training settings
dist_params:
  backend: nccl
  port: 29500
